# Contact form security token
# Must match CONTACT_TOKEN on the Railway relay server
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PUBLIC_CONTACT_TOKEN=your-secret-token-here
